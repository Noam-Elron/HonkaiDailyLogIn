import { StarRailer } from "./star_rail_gateway";

async function assert_req_settings(user) 
{
    if (typeof(cookies) !== "string")
    {
        throw new Error("Invalid Cookies Input")
    } 
}

async function main() 
{
    let user = StarRailer(cookies, user_id);
```
}